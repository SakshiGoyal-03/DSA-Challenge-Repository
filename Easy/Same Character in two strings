class Solution 
{ 
    int sameChar(String A, String B) 
    {
        int count= 0;
        A = A.toUpperCase();
        B = B.toUpperCase();
        for(int i=0;i<A.length();i++)
        {
            if(A.charAt(i)==B.charAt(i))
            {
                count++;
            }
        }
        return count;
    }
} 
